VITE_SANITY_PROJECT_ID=
VITE_SANITY_DATASET=
# Use a dated version Sanity has released, e.g. 2024-01-01 (avoid far-future dates for CDN stability).
VITE_SANITY_API_VERSION=2024-01-01
# Only if dataset is non-public / API requires auth in the browser — create a Viewer token in Sanity.
VITE_SANITY_TOKEN=